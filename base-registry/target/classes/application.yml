server:
  port: 11000
spring:
  profiles:
    active: dev
  application:
    name: base-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka
    register-with-eureka: false
    fetch-registry: false
